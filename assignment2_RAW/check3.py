# import matplotlib.pyplot as plt
# import numpy as np

# def f(x,func):
    # sum=0
    # for i in range(len(func)):
        # sum+=func[i]*pow(x,i)
        
    # return sum

# def plot(func,x_val,y_val,a,b):
    # x_1=np.linspace(a,b,100)
    # y_1=[]

    # for i in range(len(x_1)):
        # y_1.append(f(x_1[i],func))
        
    # plt.scatter(x_val,y_val)
    # plt.plot(x_1,y_1,'r')
    # plt.legend(["Data Points","Polynomial"])
    # # plt.plot()

    # plt.show()